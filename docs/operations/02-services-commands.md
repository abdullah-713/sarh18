# ⚙️ الخدمات والأوامر — وصفات العمل

> **الإصدار:** 3.4.1 | **آخر تحديث:** 2026-02-13
> **القاعدة:** كل خدمة موثقة كـ "وصفة طبخ" — المدخلات، الخطوات بالتفصيل، المخرجات، الآثار الجانبية.
> "لو ما قدرت تفهم الخطوات — أنا فاشل في الشرح مو أنت."

---

## 📑 فهرس

| # | الخدمة/الأمر | الغرض |
|---|-------------|-------|
| 1 | [AttendanceService](#1-attendanceservice) | تسجيل الحضور والانصراف |
| 2 | [GeofencingService](#2-geofencingservice) | حساب المسافة والسياج الجغرافي |
| 3 | [TrapResponseService](#3-trapresponseservice) | المصائد النفسية |
| 4 | [FinancialReportingService](#4-financialreportingservice) | التقارير المالية والتنبؤات |
| 5 | [AnalyticsService](#5-analyticsservice) | التحليلات والأنماط والتنبيهات |
| 6 | [FormulaEngineService](#6-formulaengineservice) | محرك الصيغ الحسابية |
| 7 | [SarhInstallCommand](#7-sarhinstallcommand) | تثبيت النظام |
| 8 | [GeneratePayrollCommand](#8-generatepayrollcommand) | توليد مسيرات الرواتب |
| 9 | [GenerateDailyAnalyticsCommand](#9-generatedailyanalyticscommand) | توليد التحليلات اليومية |

---

---

## 1. AttendanceService

📍 **الملف:** `app/Services/AttendanceService.php`
📋 **الغرض:** تسجيل حضور وانصراف الموظفين مع التحقق من GPS والمحرك المالي
🔴 **الأهمية:** أهم خدمة في النظام — كل شي يبدأ من هنا

### 🔧 الاعتمادية المحقونة (Constructor Injection)
```php
public function __construct(
    private GeofencingService $geofencing
)
```

---

### 📌 الدالة: `checkIn(User, float $lat, float $lng, ?string $ip, ?string $device): AttendanceLog`

**الوصفة خطوة بخطوة:**

```
📥 المدخلات: الموظف + إحداثيات GPS + (اختياري: IP + جهاز)
📤 المخرجات: سجل حضور جديد (AttendanceLog)

⚙️ الخطوات:
┌─────────────────────────────────────────────────────────────────┐
│ 1. جلب فرع الموظف                                               │
│    ↳ لو ما عنده فرع → RuntimeException                          │
│                                                                  │
│ 2. التحقق من الموقع عبر GeofencingService                       │
│    ↳ validatePosition() → {distance_meters, within_geofence}    │
│                                                                  │
│ 3. فحص استثناءات الحضور                                         │
│    ↳ AttendanceException::getActiveForUser($userId)              │
│    ↳ لو عنده استثناء bypass_geofence → يتجاوز السياج           │
│                                                                  │
│ 4. فحص صلاحية bypass-geofence (Gate — المستوى 10)              │
│    ↳ لو المستوى 10 → يتجاوز السياج تلقائياً                    │
│                                                                  │
│ 5. لو خارج السياج وما عنده تجاوز                                │
│    ↳ throw OutOfGeofenceException                                │
│                                                                  │
│ 6. تحديد وقت بداية المناوبة (بالأولوية):                        │
│    ① استثناء الحضور → custom_shift_start                        │
│    ② مناوبة الموظف → user->currentShift()->start_time           │
│    ③ الفرع → branch->default_shift_start                        │
│                                                                  │
│ 7. تحديد فترة السماح (بالأولوية):                               │
│    ① استثناء → custom_grace_minutes                              │
│    ② مناوبة الموظف → shift->grace_period_minutes               │
│    ③ الفرع → branch->grace_period_minutes                       │
│    ④ fallback → 5 دقائق                                        │
│                                                                  │
│ 8. إنشاء سجل الحضور (AttendanceLog::create)                   │
│    ↳ يحفظ: الإحداثيات، المسافة، الوقت، IP، الجهاز             │
│                                                                  │
│ 9. evaluateAttendance($shiftStart, $grace)                      │
│    ↳ يحسب delay_minutes والحالة (present/late)                  │
│                                                                  │
│ 10. لو الاستثناء عنده bypass_late_flag وكان متأخر              │
│     ↳ يغير الحالة إلى 'present'                                 │
│     ↳ يصفر delay_minutes                                        │
│     ↳ يضيف ملاحظة عربية "تم تطبيق الاستثناء..."               │
│                                                                  │
│ 11. calculateFinancials()                                        │
│     ↳ يأخذ snapshot من cost_per_minute                          │
│     ↳ يحسب delay_cost, early_leave_cost, overtime_value         │
│                                                                  │
│ 12. يحفظ ويرجع السجل                                           │
└─────────────────────────────────────────────────────────────────┘
```

**📦 الآثار الجانبية:**
- إدراج سجل في `attendance_logs`
- قراءة من `branches`, `attendance_exceptions`, `shifts`, `user_shifts`

**⚠️ أخطاء محتملة:**
- `RuntimeException` — الموظف بدون فرع
- `OutOfGeofenceException` — خارج السياج
- `ModelNotFoundException` — لو ما لقى بيانات ضرورية

---

### 📌 الدالة: `checkOut(User, float $lat, float $lng): AttendanceLog`

**الوصفة:**

```
📥 المدخلات: الموظف + إحداثيات GPS
📤 المخرجات: سجل الحضور المحدّث

⚙️ الخطوات:
┌───────────────────────────────────────────────────────────────┐
│ 1. جلب سجل حضور اليوم (firstOrFail)                          │
│    ↳ لو ما في check-in → ModelNotFoundException               │
│                                                                │
│ 2. التحقق من الموقع عبر GeofencingService                     │
│                                                                │
│ 3. تسجيل بيانات الخروج:                                       │
│    ↳ check_out_at = now()                                      │
│    ↳ check_out_latitude, longitude, distance, within_geofence │
│                                                                │
│ 4. حساب دقائق العمل:                                          │
│    ↳ worked_minutes = check_out_at - check_in_at               │
│                                                                │
│ 5. تحديد المدة المتوقعة:                                      │
│    ↳ المناوبة → duration_minutes                               │
│    ↳ أو الفرع → calculateBranchShiftMinutes()                 │
│                                                                │
│ 6. المقارنة:                                                   │
│    ↳ worked > expected → overtime_minutes = الفرق              │
│    ↳ worked < expected → early_leave_minutes = الفرق           │
│                                                                │
│ 7. إعادة حساب التكاليف (بالـ snapshot المحفوظ):               │
│    ↳ delay_cost = delay_minutes × cost_per_minute              │
│    ↳ early_leave_cost = early_leave × cost_per_minute          │
│    ↳ overtime_value = overtime × cost_per_minute × 1.5         │
│                                                                │
│ 8. حفظ التحديث                                                │
└───────────────────────────────────────────────────────────────┘
```

**📌 ملاحظة مهمة:** `cost_per_minute` لا يُعاد حسابه — يستخدم القيمة المحفوظة عند check-in (اللقطة المالية)

---

### 📌 الدالة الخاصة: `calculateBranchShiftMinutes($branch): int`
- يحسب مدة المناوبة من أوقات الفرع الافتراضية
- يدعم المناوبات الليلية (start > end)
- الافتراضي: 480 دقيقة (8 ساعات)

---

---

## 2. GeofencingService

📍 **الملف:** `app/Services/GeofencingService.php`
📋 **الغرض:** حساب المسافة بين الموظف والفرع والتحقق من السياج
🟡 **الأهمية:** مكوّن أساسي يستخدمه AttendanceService

### 📌 الدالة: `validatePosition(Branch, float $lat, float $lng): array`

```
📥 المدخلات: الفرع + إحداثيات الموظف
📤 المخرجات: [distance_meters => float, within_geofence => bool]

⚙️ الخطوات:
1. حساب المسافة بصيغة Haversine: branch->distanceTo($lat, $lng)
2. مقارنة المسافة مع geofence_radius
3. إرجاع النتيجة
```

### 📌 الدالة الثابتة: `haversineDistance($lat1, $lng1, $lat2, $lng2): float`

```
صيغة Haversine — حساب المسافة على سطح الكرة:
R = 6,371,000 متر (نصف قطر الأرض)
a = sin²(Δlat/2) + cos(lat1) × cos(lat2) × sin²(Δlng/2)
c = 2 × asin(√a)
distance = R × c

النتيجة بالأمتار، مقربة لخانتين عشريتين
```

**📌 مثال عملي:**
```
الفرع: lat=21.4858, lng=39.1925
الموظف: lat=21.4859, lng=39.1926
المسافة = ~14.2 متر
geofence_radius = 17 متر
النتيجة: within_geofence = true ✅
```

---

---

## 3. TrapResponseService

📍 **الملف:** `app/Services/TrapResponseService.php`
📋 **الغرض:** إدارة المصائد النفسية — التسجيل والردود المزيفة
🔴 **الأهمية:** نظام أمني حساس

### 📌 الدالة: `triggerTrap(User, string $trapCode, Request): array`

```
📥 المدخلات: الموظف + كود المصيدة + طلب HTTP
📤 المخرجات: [response => array, risk_score => float]

⚙️ الخطوات:
┌──────────────────────────────────────────────────────────┐
│ 1. البحث عن المصيدة بالكود (Trap::where('trap_code'))   │
│    ↳ ما لقى → ModelNotFoundException                     │
│                                                          │
│ 2. إنشاء سجل التفاعل (TrapInteraction::create):         │
│    ↳ user_id, trap_id, trap_type                         │
│    ↳ page_url (من request)                               │
│    ↳ ip_address, user_agent                              │
│    ↳ interaction_data (JSON)                              │
│    ↳ risk_level = trap->deriveRiskLevel()                │
│                                                          │
│ 3. زيادة درجة الخطورة للموظف:                           │
│    ↳ user->incrementRiskScore()                          │
│    ↳ الصيغة: risk_score = 10 × (2^n − 1)              │
│                                                          │
│ 4. توليد رد مزيف:                                       │
│    ↳ generateFakeResponse(trap)                          │
│                                                          │
│ 5. إرجاع الرد + درجة الخطورة الجديدة                    │
└──────────────────────────────────────────────────────────┘
```

### 📌 الدالة: `generateFakeResponse(Trap): array`

**الردود حسب كود المصيدة:**

| كود المصيدة | الرد المزيف | التفاصيل |
|-------------|------------|----------|
| `SALARY_PEEK` | جدول رواتب وهمي | 3 موظفين بأسماء وهمية ورواتب عشوائية (4000-15000) |
| `PRIVILEGE_ESCALATION` | رسالة نجاح | "تم منح الصلاحية" (وهمي) |
| `SYSTEM_BYPASS` | رسالة تحذير | "النظام متوقف مؤقتاً" (وهمي) |
| `DATA_EXPORT` | رابط تحميل | ملف CSV وهمي بنسبة 100% — لا يوجد فعلياً |
| افتراضي | رسالة عامة | يستخدم `fake_response_type` من المصيدة |

**📌 الفكرة:** الموظف يظن إنه نجح في التحايل، لكن:
- كل شي مسجل في `trap_interactions`
- درجة الخطورة ارتفعت
- المدير يشوف التقرير في `TrapAuditPage`

---

---

## 4. FinancialReportingService

📍 **الملف:** `app/Services/FinancialReportingService.php`
📋 **الغرض:** التقارير المالية — الخسائر اليومية، أداء الفروع، تحليل الأثر، التنبؤات
🟡 **الأهمية:** عالية — يغذي لوحة التحكم المالية

### ⏰ التخزين المؤقت
```php
const CACHE_TTL = 300; // 5 دقائق
```
كل الدوال تستخدم Cache لتقليل الحمل على قاعدة البيانات.

---

### 📌 الدالة: `getDailyLoss(Carbon $date, ?int $branchId): float`

```
📥 المدخلات: التاريخ + (اختياري: رقم الفرع)
📤 المخرجات: مجموع تكلفة التأخير (ريال)

⚙️ المنطق:
1. مفتاح الكاش: sarh.loss.{YYYY-MM-DD}.{branchId|all}
2. استعلام: AttendanceLog→where(date)→sum(delay_cost)
3. كاش 5 دقائق

مثال: getDailyLoss(today(), 1) → 345.50 ريال
```

---

### 📌 الدالة: `getBranchPerformance(Carbon $month): Collection`

```
📥 المدخلات: الشهر
📤 المخرجات: مجموعة من صفوف أداء الفروع

⚙️ لكل فرع نشط:
1. جلب سجلات الحضور للشهر
2. إحصاء: الحاضرين، المتأخرين، الغائبين
3. إحصاء: انتهاكات السياج الجغرافي
4. مجموع: تكلفة التأخير
5. حساب: نسبة الحضور في الوقت
6. حساب: نسبة الامتثال للسياج
7. التقييم:
   ≥ 95% → excellent (ممتاز)
   ≥ 85% → good (جيد)
   ≥ 70% → average (متوسط)
   < 70% → poor (ضعيف)
```

---

### 📌 الدالة: `getDelayImpactAnalysis(start, end, scope, ?scopeId): array`

```
📥 المدخلات: فترة + نطاق (company/branch/department/employee)
📤 المخرجات: {potential_loss, actual_loss, roi_percentage, discipline_savings, total_delay_minutes}

⚙️ المنطق:
1. جلب سجلات الحضور المفلترة بالنطاق
2. actual_loss = مجموع delay_cost
3. potential_loss = مجموع (delay_minutes × cost_per_minute) × 1.3
   ↳ ال 30% زيادة = نمذجة سيناريو "بدون نظام تلعيب"
4. roi = (potential - actual) / potential × 100
5. savings = potential - actual

📌 هذا يجاوب على: "كم وفرنا بسبب نظام صرح؟"
```

---

### 📌 الدالة: `getPredictiveMonthlyLoss(Carbon $month): array`

```
📥 المدخلات: الشهر
📤 المخرجات: {avg_daily, accumulated, elapsed_days, remaining_days, predicted_total}

⚙️ المنطق:
1. عدّ أيام العمل المنقضية (تواريخ حضور فريدة)
2. مجموع الخسائر المتراكمة = sum(delay_cost)
3. المتوسط اليومي = المتراكم ÷ الأيام المنقضية
4. الأيام المتبقية = 22 − المنقضية
5. التنبؤ = المتراكم + (المتوسط × المتبقية)

📌 مثال: 
  أيام العمل المنقضية = 10، المتراكم = 5000 ريال
  المتوسط = 500 ريال/يوم
  المتبقي = 12 يوم
  التنبؤ = 5000 + (500 × 12) = 11,000 ريال
```

---

---

## 5. AnalyticsService

📍 **الملف:** `app/Services/AnalyticsService.php`
📋 **الغرض:** محرك التحليلات الشامل — لقطات، تنبيهات، أنماط، تقييمات
🔴 **الأهمية:** أعقد خدمة — 15+ دالة

---

### 📌 `calculateVPM(Branch, ?string $period): float`
**القيمة لكل دقيقة (Value Per Minute)**

```
⚙️ المنطق:
VPM = ميزانية الرواتب ÷ (عدد الموظفين × أيام العمل × ساعات × 60)

مثال:
  ميزانية = 250,000 ريال
  موظفين = 30، أيام = 22، ساعات = 8
  إجمالي الدقائق = 30 × 22 × 8 × 60 = 316,800
  VPM = 250,000 ÷ 316,800 = 0.7891 ريال/دقيقة
```

---

### 📌 `calculateTotalLoss(Branch, Carbon $date): array`
**حساب الخسائر الإجمالية ليوم واحد**

```
📤 المخرجات: {delay_losses, absence_losses, early_leave_losses, total_losses,
             absent_count, present_count, late_count, total_delay_minutes}

⚙️ الخطوات:
1. لو جمعة أو سبت أو إجازة → أصفار
2. delay_losses = sum(delay_cost) من سجلات اليوم
3. early_leave_losses = sum(early_leave_cost)
4. absent_count = active_employees − distinct(present users)
5. absence_losses = absent_count × shift_minutes × VPM
6. total = delay + early + absence
```

---

### 📌 `calculateEfficiencyScore(Branch, start, end): float`
**درجة الكفاءة المركبة (0-100)**

```
⚙️ المعادلة المركبة:
score = (attendance × 0.4) + (punctuality × 0.3) + (retention × 0.2) + (consistency × 0.1)

المكونات:
• attendance_rate (40%) = أزواج (تاريخ+موظف) ÷ (الموظفين × أيام العمل)
• punctuality (30%) = 100 − (متوسط التأخير × 3.33)
  ↳ 30 دقيقة تأخير = صفر نقاط
  ↳ 0 دقائق = 100 نقطة
• retention (20%) = إجمالي worked_minutes ÷ expected_minutes
• consistency (10%) = أيام بدون خسائر ÷ إجمالي الأيام
```

---

### 📌 `calculateROIMatrix(?start, ?end): array`
**مصفوفة عائد الاستثمار — مستوحاة من BCG Matrix**

```
📤 المخرجات: [{branch_id, name, budget, losses, roi, efficiency, quadrant}]

⚙️ التصنيف:
┌─────────────────┬──────────────┬──────────────┐
│                 │ كفاءة ≥ 70  │ كفاءة < 70  │
├─────────────────┼──────────────┼──────────────┤
│ ROI ≥ 90%      │ ⭐ نجم      │ 🐄 بقرة نقد │
├─────────────────┼──────────────┼──────────────┤
│ ROI < 90%      │ 📈 واعد     │ ⚠️ خطر      │
└─────────────────┴──────────────┴──────────────┘
```

---

### 📌 `generateHeatmapData(Branch, start, end): array`
**بيانات الخريطة الحرارية**

```
📤 المخرجات:
{
  hourly_distribution: {6: 5, 7: 12, 8: 45, ...},  // عدد check-ins لكل ساعة
  daily_distribution: {
    0: {present: 20, late: 5, absent: 2},  // الأحد
    1: {present: 22, late: 3, absent: 1},  // الاثنين
    ...
  }
}
```

---

### 📌 `detectFrequentLatePattern(Branch): Collection`
**كشف نمط التأخير المتكرر**

```
⚙️ المنطق:
1. آخر 14 يوم
2. الموظفين بـ ≥3 تأخيرات
3. frequency_score = (عدد التأخيرات ÷ 10) × 100 (max 100)
4. financial_impact = مجموع delay_cost
5. risk: score ≥ 70 → high, ≥ 40 → medium, else → low
6. upsert في EmployeePattern (type: frequent_late)
```

---

### 📌 `detectPreHolidayPattern(Branch): Collection`
**كشف نمط الغياب قبل/بعد الإجازات**

```
⚙️ المنطق:
1. جلب إجازات آخر 3 أشهر
2. لكل موظف: فحص الغياب يوم قبل وبعد كل إجازة
3. rate = (غيابات ÷ فرص) × 100
4. rate ≥ 50% → يُسجل كنمط
5. risk: rate ≥ 80% → critical, ≥ 60% → high, else → medium
```

---

### 📌 `detectMonthlyCyclePattern(Branch): Collection`
**كشف النمط الأسبوعي/الشهري**

```
⚙️ المنطق:
1. آخر 3 أشهر، الموظفين بـ ≥20 سجل
2. تجميع حسب يوم الأسبوع
3. أيام بـ ≥4 تأخيرات و ≥60% نسبة تأخير
4. لو في أيام مشكلة → upsert EmployeePattern (type: monthly_cycle)
```

---

### 📌 `checkAndTriggerAlerts(Branch, Carbon $date): array`
**التحقق من الحدود وإطلاق التنبيهات**

```
⚙️ ثلاثة أنواع تنبيهات:

1. تجاوز حد الخسائر:
   ↳ لو daily_loss_rate > max_acceptable_loss_percent
   ↳ severity: > 2× الحد → critical, else → high

2. انخفاض الحضور:
   ↳ لو attendance < (target − 10%)
   ↳ severity: < (target − 20%) → critical, else → high

3. تأخير جماعي:
   ↳ لو > 30% من الموظفين متأخرين
   ↳ severity: high
```

---

### 📌 `generateDailySnapshot(Branch, ?Carbon $date): AnalyticsSnapshot`
**توليد لقطة يومية شاملة**

```
⚙️ يجمع كل الدوال السابقة:
1. calculateTotalLoss() → الخسائر
2. calculateEfficiencyScore() → الكفاءة
3. calculateVPM() → القيمة/دقيقة
4. calculateProductivityGap() → فجوة الإنتاجية
5. generateHeatmapData() → الخريطة الحرارية
6. upsert في AnalyticsSnapshot
```

---

### 📌 `runFullAnalysis(?Carbon $date): array`
**التحليل الشامل — يُستدعى من الأمر اليومي**

```
⚙️ لكل فرع نشط:
1. generateDailySnapshot() → لقطة
2. checkAndTriggerAlerts() → تنبيهات
3. (يوم الأحد فقط): كشف الأنماط الثلاثة
   ↳ detectFrequentLatePattern()
   ↳ detectPreHolidayPattern()
   ↳ detectMonthlyCyclePattern()
4. في حالة خطأ: يسجل الخطأ ويكمل للفرع التالي
```

---

### 📌 `getPersonalMirror(User): array`
**المرآة الشخصية — للموظف في PWA**

```
📤 المخرجات:
{
  performance_score: 87.5,
  present_days: 18,
  late_days: 2,
  absent_days: 1,
  total_delay: 25 دقيقة,
  total_loss: 18.94 ريال,
  streak: 5 أيام بدون تأخير,
  branch_rank: 3 من 30,
  message: "أداؤك ممتاز! استمر...",
  attendance_rate: 90%,
  punctuality_rate: 85%
}

⚙️ المنطق:
1. سجلات الشهر الحالي
2. إحصائيات الحضور والتأخير
3. حساب streak بالمشي للخلف في السجلات
4. الترتيب بين موظفي الفرع (أقل تأخير = أعلى)
5. performance = attendance × 0.6 + punctuality × 0.4
6. رسالة تحفيزية عربية حسب المستوى
```

---

### 📌 `getLostOpportunityClock(): array`
**ساعة الفرص الضائعة — للوحة التحكم الرئيسية**

```
📤 المخرجات:
{
  total_loss_today: 2,345.00 ريال,
  total_delay_minutes: 342 دقيقة,
  total_absent: 5,
  branch_breakdown: [top 5 فروع بأعلى خسائر],
  timestamp: الآن
}
```

---

---

## 6. FormulaEngineService

📍 **الملف:** `app/Services/FormulaEngineService.php`
📋 **الغرض:** تقييم الصيغ الحسابية المخصصة مع بيانات حقيقية من النظام
🟡 **الأهمية:** يغذي تقارير الأداء المخصصة

---

### 📌 `evaluateForUser(ReportFormula, User, start, end): ?float`

```
⚙️ الخطوات:
1. resolveVariablesForUser() → يجلب القيم الحقيقية
2. formula->evaluate($values) → يقيّم الصيغة

مثال:
  الصيغة: "(attendance × 0.6) + (on_time_rate × 0.4)"
  المتغيرات: {attendance: 95.0, on_time_rate: 88.0}
  النتيجة: (95 × 0.6) + (88 × 0.4) = 57 + 35.2 = 92.2
```

---

### 📌 `evaluateForBranch(ReportFormula, branchId, start, end): array`

```
⚙️ المنطق:
1. جلب كل موظفي الفرع النشطين
2. تقييم الصيغة لكل موظف
3. ترتيب تنازلي حسب النتيجة
📤 المخرجات: [{user: User, score: float}]
```

---

### 📌 `resolveVariablesForUser(User, start, end, requiredVars): array`

**المتغيرات المتاحة:**

| المتغير | القيمة | المصدر |
|---------|-------|--------|
| `attendance` | نسبة الحضور (%) | سجلات الحضور |
| `delay_rate` | نسبة التأخير (%) | سجلات الحضور |
| `on_time_rate` | نسبة الحضور في الوقت (%) | سجلات الحضور |
| `total_points` | مجموع النقاط | User.total_points |
| `overtime_rate` | نسبة العمل الإضافي (%) | سجلات الحضور |
| `early_leave_rate` | نسبة المغادرة المبكرة (%) | سجلات الحضور |
| `financial_loss` | الخسارة المالية | sum(delay_cost) |
| `task_completion` | إنجاز المهام | 100.0 (placeholder) |
| `current_streak` | السلسلة الحالية | User.current_streak |
| `longest_streak` | أطول سلسلة | User.longest_streak |
| `total_delay_minutes` | دقائق التأخير | sum(delay_minutes) |
| `total_overtime_minutes` | دقائق العمل الإضافي | sum(overtime_minutes) |
| `manual_adjustments` | تعديلات يدوية | ScoreAdjustment.sum(points_delta) |

---

---

## 7. SarhInstallCommand

📍 **الملف:** `app/Console/Commands/SarhInstallCommand.php`
📋 **الأمر:** `php artisan sarh:install`
📋 **الغرض:** تثبيت النظام من الصفر

### ⚙️ الخطوات

```
الخطوة 1: التحقق من البيئة
├── PHP Extensions: openssl, pdo, mbstring, tokenizer, xml, ctype, json, bcmath
├── مفتاح التطبيق: يولّد لو ما موجود
└── اتصال قاعدة البيانات: يختبر الاتصال

الخطوة 2: ترحيل قاعدة البيانات
└── php artisan migrate --force

الخطوة 3: بذر البيانات الأساسية
├── RolesAndPermissionsSeeder (الأدوار والصلاحيات)
├── BadgesSeeder (الشارات)
└── TrapsSeeder (المصائد)

الخطوة 4: إنشاء المدير الأول (لو ما موجود)
├── يسأل: الاسم العربي، الإنجليزي، البريد، كلمة المرور
├── ينشئ User مع employee_id = SARH-{year}-0001
├── promoteToSuperAdmin() → security_level = 10
└── يعيّن دور super_admin

الخطوة 5: التهيئة النهائية
├── storage:link (ربط التخزين)
└── (في الإنتاج): config:cache, route:cache, view:cache
```

---

---

## 8. GeneratePayrollCommand

📍 **الملف:** `app/Console/Commands/GeneratePayrollCommand.php`
📋 **الأمر:** `php artisan sarh:payroll [--period=YYYY-MM] [--branch=ID]`
📋 **الغرض:** توليد مسيرات الرواتب لشهر كامل

### ⚙️ الخطوات

```
1. تحديد الفترة: --period أو الشهر السابق
2. تحديد الفرع: --branch أو كل الفروع
3. جلب الموظفين النشطين (مفلتر حسب الفرع لو محدد)
4. لكل موظف:
   ├── Payroll::generateForUser($user, $period)
   ├── نجاح → +1 success
   └── خطأ → طباعة الخطأ +1 error
5. طباعة الملخص: ✓ نجح X / ✗ فشل Y
```

**📌 ملاحظة:** يستخدم `updateOrCreate` — آمن للتشغيل المتكرر (idempotent)

---

---

## 9. GenerateDailyAnalyticsCommand

📍 **الملف:** `app/Console/Commands/GenerateDailyAnalyticsCommand.php`
📋 **الأمر:** `php artisan sarh:analytics [--date=YYYY-MM-DD]`
📋 **الغرض:** توليد تحليلات يومية لكل الفروع

### ⚙️ الخطوات

```
1. تحديد التاريخ: --date أو اليوم
2. استدعاء AnalyticsService::runFullAnalysis($date)
   ├── لكل فرع: snapshot + alerts + (أحد: patterns)
3. عرض النتائج في جدول:
   Branch | Status | Snapshot | Alerts | Patterns | Error
4. طباعة الملخص

📌 يُشغّل يومياً عبر Cron:
   * * * * * cd /path && php artisan sarh:analytics >> /dev/null 2>&1
   أو عبر Laravel Scheduler
```

---

---

## 📡 المسارات (Routes)

📍 **الملف:** `routes/web.php`

### المسارات العامة (بدون مصادقة)

| الطريقة | المسار | الغرض |
|---------|--------|-------|
| `GET /` | إعادة توجيه لـ `/admin/login` | الصفحة الرئيسية |
| `GET /manifest.json` | ملف PWA Manifest ديناميكي | إعدادات من Setting |
| `GET /sw.js` | Service Worker | تخزين مؤقت + دعم offline |
| `GET /whistleblower` | نموذج البلاغ المجهول | **بدون مصادقة عمداً!** |
| `GET /whistleblower/track` | متابعة البلاغ بالرمز | **بدون مصادقة عمداً!** |

### مسارات الموظف (تتطلب مصادقة)

| الطريقة | المسار | الغرض | الاسم |
|---------|--------|-------|-------|
| `GET /dashboard` | لوحة الموظف (EmployeeDashboard) | `dashboard` |
| `GET /messaging` | صندوق الرسائل | `messaging.inbox` |
| `GET /messaging/{conversation}` | المحادثة | `messaging.chat` |
| `POST /attendance/check-in` | تسجيل الحضور GPS | `attendance.check_in` |
| `POST /attendance/check-out` | تسجيل الانصراف GPS | `attendance.check_out` |
| `GET /attendance/today` | حالة حضور اليوم | `attendance.today` |
| `POST /traps/trigger` | تفعيل المصيدة | `traps.trigger` |

### ⚠️ ملاحظات تصميمية مهمة

1. **البلاغات بدون مصادقة:** عمداً — لحماية هوية المبلغ. لا IP logging، لا sessions.
2. **الـ PWA Manifest ديناميكي:** يقرأ من `Setting::instance()` — لو تغير الاسم أو اللون يتحدث فوراً.
3. **الـ Service Worker:** يدعم offline fallback — لو الموظف فقد الاتصال يبقى التطبيق شغال.
4. **مسار المصائد:** يتطلب مصادقة — يسجل المستخدم الحالي كمتفاعل.

---

> **صرح الإتقان v3.4.1** — *"كل خدمة وصفة. كل خطوة مشروحة. كل أثر جانبي موثق."*
